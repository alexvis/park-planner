<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Park Planner" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="row">

    <nav>
    <div class="top-bar">
      <div class="top-bar-left">
        <ul class="menu">
          <% if user_signed_in?  %>
            <li class="left"><%= link_to 'Home', user_path(current_user) %></li>
            <% if current_user.admin? %>
              <li class="right"><%= link_to 'Admin', users_path %></li>
            <% end %>
            <li class="right"><%= link_to 'My Account', edit_user_path(current_user) %></li>
            <li class="right"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li class="left"><%= link_to "Sign In", new_user_session_path %></li>
            <li class="left"><%= link_to "Sign Up", new_user_registration_path %></li>
          <% end %>
        </ul>
    </div>
  </div>
  </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
